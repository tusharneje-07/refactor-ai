info:
  name: /_dev_flush_data
  type: http
  seq: 6

http:
  method: POST
  url: http://127.0.0.1:8000/_dev_flush_data
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
